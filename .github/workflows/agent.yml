name: Trading Agent

on:
  schedule:
    - cron: '*/10 * * * 1-5'   # Co 10 minut, pon-pt
  workflow_dispatch:

jobs:
  run-agent:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
    - uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: pip install requests pytz numpy

    - name: Run agent
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
      run: python agent.py

    - - name: Commit changes
      run: |
        git config --global user.name 'GitHub Actions Bot'
        git config --global user.email 'actions@github.com'
        # Dodaj wszystkie zmiany, ale nie przerywaj na błędzie
        git add -A
        # Jeśli nie ma zmian, commit się nie powiedzie – to OK
        git commit -m 'Auto-update data' || echo "No changes to commit"
        git push || echo "Push failed"
